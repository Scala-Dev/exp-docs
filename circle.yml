machine:
  node:
    version: 8.9.4
  environment:
    PATH: "${PATH}:${HOME}/${CIRCLE_PROJECT_REPONAME}/node_modules/.bin"
  services:
    - docker
dependencies:
  override:
    - echo
compile:
  override:
    - /bin/bash ./circle.sh
test:
  override:
    - echo
